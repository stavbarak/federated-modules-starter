{
  "name": "admin_center",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "concurrently \"wsrun --parallel start\" \"wsrun --parallel serve\""
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "wsrun": "^5.2.0"
  },
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
